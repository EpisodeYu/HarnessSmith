[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = "Agent harness generated by HarnessForge — you own this code."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
# Scaffold generated by HarnessForge (Slice 0). Runtime deps (openai, pydantic, ...)
# arrive in Slice 1. No agent-framework lock-in: this project intentionally has
# zero dependency on any agent-orchestration framework.
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/{{ project_slug }}"]
