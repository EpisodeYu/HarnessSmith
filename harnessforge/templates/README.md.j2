# {{ project_slug }}

> Generated by [HarnessForge](https://github.com/EpisodeYu/HarnessForge) — an agent harness you own. No agent-framework lock-in (no LangChain / LangGraph / ADK), and no runtime dependency on HarnessForge.

This is a **scaffold** (HarnessForge Slice 0). The runnable harness — the
function-calling loop, tool registry, LLM client, trace, and CLI — is filled in
by later generation stages.

## Layout

```
src/{{ project_slug }}/      # your package
harness.spec.yaml            # snapshot of the spec this repo was generated from
.env.example                 # copy to .env and fill in real secret values
```

## Secrets

Secrets never touch this repo's git history. `harness.spec.yaml` and config hold
only environment-variable *names*; the real values live in `.env`, which is
gitignored.

```bash
cp .env.example .env   # then edit .env with your real keys
```
