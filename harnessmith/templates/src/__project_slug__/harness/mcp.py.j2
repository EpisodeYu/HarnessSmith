"""Optional MCP tool bridge (generated only when ``mcp.enabled``).

MCP tools register into the SAME :class:`~{{ project_slug }}.harness.tools.Registry`
as built-in tools and run through the SAME loop/trace — ``loop.py`` is untouched.
*Which* servers to connect to and *which* tools to allowlist are runtime
``config.yaml`` knobs; transport is chosen per server by shape (``command`` ->
stdio subprocess, ``url`` -> remote Streamable HTTP).

The MCP SDK is asyncio; all of it hides behind a synchronous ``McpManager.call``.
One background thread owns one event loop; each server gets its OWN task that
enters, USES, and exits that server's session (anyio requires a session be
entered, used, and exited in the *same* task — so each server's ``call_tool`` runs
in that task, with the dispatcher only routing requests to it). Servers connect
concurrently, so startup costs ``max`` per-server connect time, not the sum.
``begin()`` starts that without blocking (so a UI can show a live "connecting"
state); ``reconnect_server()`` restarts just one server in place. stdio servers
launched via ``uvx``/``npx`` fetch their package through a reachable registry +
the system proxy, auto-resolved by probing (official, else a domestic mirror;
directly or via proxy) so they work behind the GFW or a corporate proxy; a missing
launcher fails with an actionable message instead of a raw ``WinError 2``.
"""

from __future__ import annotations

import asyncio
import concurrent.futures
import threading
import time
from contextlib import AsyncExitStack
from typing import TYPE_CHECKING, Any

from .debuglog import log
from .tools import HIGH, SAFE, Registry, Tool
from .tools import registry as default_registry

if TYPE_CHECKING:
    from .config import Config, McpConfig, McpServerConfig


def _bearer_headers(server: McpServerConfig) -> dict[str, str]:
    """Build request headers for a remote server, resolving the token by NAME."""
    from .config import resolve_env

    token = resolve_env(server.auth_env) if server.auth_env else None
    return {"Authorization": f"Bearer {token}"} if token else {}


_WINDOWS_CMD_SHIMS = {"npx", "npm", "yarn", "pnpm"}
_PYPI_OFFICIAL = "https://pypi.org/simple"
_PYPI_MIRROR = "https://pypi.tuna.tsinghua.edu.cn/simple"
_NPM_OFFICIAL = "https://registry.npmjs.org"
_NPM_MIRROR = "https://registry.npmmirror.com"

# Skip the bundled-browser download that a Node package's (transitive) postinstall
# would otherwise run during `npm install` — puppeteer/playwright fetch a ~150MB
# Chrome from Google's CDN (e.g. desktop-commander -> md-to-pdf -> puppeteer). That
# download is unreachable behind the GFW and FAILS the whole install (exit 1 ->
# incomplete package tree -> the later `node <bin>` launch crashes on a missing
# module -> "Connection closed"). None of the prefilled tools need a bundled
# browser (desktop-commander's terminal/file tools and open-websearch's request
# scrapers all work without one), so skipping the download lets BOTH the install
# and the launch succeed. Injected with `setdefault`, so a user who actually wants
# the browser can pre-set any of these to keep the download.
_NODE_INSTALL_SKIP_DOWNLOAD = {
    "PUPPETEER_SKIP_DOWNLOAD": "true",
    "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD": "true",  # older puppeteer's env-var name
    "PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD": "1",
}


def _missing_launcher_message(command: str) -> str:
    base = command.lower().removesuffix(".cmd").removesuffix(".bat").removesuffix(".exe")
    if base in _WINDOWS_CMD_SHIMS or base == "node":
        return (
            f"MCP stdio launcher {command!r} was not found on PATH. "
            "Install Node.js (which provides node/npm/npx) or edit config.yaml to use "
            "a launcher that exists on this machine."
        )
    if command.lower() == "uvx":
        return (
            "MCP stdio launcher 'uvx' was not found on PATH. Install uv or edit "
            "config.yaml to use a launcher that exists on this machine."
        )
    return f"MCP stdio launcher {command!r} was not found on PATH."


def _windows_cmd_exe() -> str:
    import os
    import shutil

    return os.environ.get("COMSPEC") or shutil.which("cmd.exe") or "cmd.exe"


def _windows_stdio_command(command: str, args: list[str]) -> tuple[str, list[str]]:
    """Make a stdio command launchable on Windows.

    ``npx`` (and ``npm``/``yarn``/``pnpm``) are usually ``.cmd`` batch shims;
    Windows ``CreateProcess`` — which the asyncio subprocess used by the MCP SDK
    calls — cannot execute a ``.cmd``/``.bat`` directly (it fails with
    ``WinError 2``), even when the file is on PATH. Hand those to ``cmd.exe``,
    which knows how to run them. Real ``.exe`` commands (``uvx``, ``python``, …)
    are launched directly and left untouched. If the launcher is missing, fail
    before entering the MCP transport so status pages show an actionable message
    instead of a raw ``WinError 2``.
    """
    import shutil
    import sys

    if sys.platform != "win32":
        return command, args
    resolved = shutil.which(command)
    if not resolved:
        raise FileNotFoundError(_missing_launcher_message(command))
    if resolved.lower().endswith((".cmd", ".bat")):
        return _windows_cmd_exe(), ["/d", "/c", resolved, *args]
    return command, args


def _base_command(command: str | None) -> str:
    """The bare command name, lower-cased and stripped of a Windows shim suffix."""
    import os

    return os.path.basename(command or "").lower().removesuffix(".cmd").removesuffix(".exe")


# --- Node (npx) servers: install once into a stable per-server dir, launch `node`
#     directly ----------------------------------------------------------------
#
# An npx server is NOT launched via ``npx`` at runtime. ``npx`` re-installs/stages
# the package into its ephemeral ``_npx`` cache on EVERY launch, and on Windows that
# stage/cleanup step trips ``EPERM: rmdir`` / ``EBUSY`` (antivirus / indexer / file
# locks) — which closes the stdio connection before the handshake. Instead we
# ``npm install`` the pinned package ONCE into a stable, project-owned prefix
# (``.harness/mcp-node/<server>``) and launch its bin DIRECTLY with ``node <bin>``:
# no ``_npx``, no per-launch staging, no cleanup — so the launch path never trips
# that error and a later (offline) connect is a true cache hit.


def _npx_package(server: McpServerConfig) -> str | None:
    """The npm package spec of an ``npx`` server (first non-flag arg), else ``None``."""
    if _base_command(server.command) != "npx":
        return None
    return next((a for a in server.args if not a.startswith("-")), None)


def _node_install_dir(name: str) -> str:
    """Stable, project-owned npm prefix for one Node server (gitignored state dir).

    Installed into ONCE (warm/prefetch) and launched from on every connect — never
    the ephemeral ``npx`` cache. Relative to the run cwd (the project root), like
    the ``.mcp-warmed`` sentinel."""
    import os

    return os.path.join(".harness", "mcp-node", name)


def _node_package_name(spec: str) -> str:
    """Package NAME from an npm spec: strip a trailing ``@version`` but keep a
    leading scope ``@`` (``@scope/pkg@1.2.3`` -> ``@scope/pkg``; ``pkg@1`` -> ``pkg``)."""
    at = spec.rfind("@")
    return spec[:at] if at > 0 else spec


def _node_requested_version(spec: str) -> str | None:
    """The pinned version of an npm spec, or ``None`` when unpinned."""
    at = spec.rfind("@")
    return spec[at + 1:] if at > 0 else None


def _node_pkg_dir(server: McpServerConfig) -> str | None:
    """``<prefix>/node_modules/<pkg>`` for an npx server (handles scoped names)."""
    import os

    pkg = _npx_package(server)
    if pkg is None:
        return None
    name = _node_package_name(pkg)
    return os.path.join(_node_install_dir(server.name), "node_modules", *name.split("/"))


def _node_manifest(server: McpServerConfig) -> dict | None:
    """The installed package's ``package.json`` (dict), or ``None`` if absent."""
    import json
    import os

    pkg_dir = _node_pkg_dir(server)
    if pkg_dir is None:
        return None
    try:
        with open(os.path.join(pkg_dir, "package.json"), encoding="utf-8") as fh:
            return json.load(fh)
    except (OSError, ValueError):
        return None


def _node_satisfied(server: McpServerConfig) -> bool:
    """True when the pinned package@version is already installed — so warm can skip
    the ``npm install`` reconcile entirely (fast AND offline-safe on every later
    connect). An unpinned spec always reconciles (it could resolve to something
    newer)."""
    pkg = _npx_package(server)
    if pkg is None:
        return False
    want = _node_requested_version(pkg)
    if not want:
        return False
    manifest = _node_manifest(server)
    return bool(manifest and manifest.get("version") == want)


def _node_install_argv(server: McpServerConfig) -> tuple[str, list[str]] | None:
    """``(npm, [install --prefix <dir> <pkg> ...])`` to install an npx server's
    package into its stable per-server prefix, or ``None`` for a non-npx server.
    ``--no-save`` keeps it a bare install; ``--loglevel error`` keeps npm's chatter
    off the warm log (and off the protocol stream — the launch is plain ``node``)."""
    pkg = _npx_package(server)
    if pkg is None:
        return None
    return "npm", [
        "install", "--prefix", _node_install_dir(server.name), pkg,
        "--no-save", "--no-fund", "--no-audit", "--loglevel", "error",
    ]


def _node_bin_path(server: McpServerConfig) -> str:
    """Absolute path to the installed package's bin JS (run as ``node <path>``).

    Reads the installed ``package.json`` ``bin`` — a string, or the entry whose key
    matches the package name in a bin MAP (else the first). Raises ``RuntimeError``
    (a RETRYABLE connect error, not a missing-launcher one) when the package isn't
    installed yet: the prefetch installs it and the connect heals on the next try."""
    import os

    pkg = _npx_package(server)
    pkg_dir = _node_pkg_dir(server)
    if pkg is None or pkg_dir is None:
        raise RuntimeError(f"mcp server {server.name!r} is not an npx package server")
    manifest = _node_manifest(server)
    if manifest is None:
        raise RuntimeError(
            f"node package for {server.name!r} is not installed yet at {pkg_dir} "
            "(warm/prefetch installs it; the connect then retries)"
        )
    name = _node_package_name(pkg)
    bin_field = manifest.get("bin")
    if isinstance(bin_field, str):
        rel = bin_field
    elif isinstance(bin_field, dict) and bin_field:
        rel = bin_field.get(name.rsplit("/", 1)[-1]) or next(iter(bin_field.values()))
    else:
        rel = manifest.get("main") or "index.js"
    return os.path.normpath(os.path.join(pkg_dir, rel))


def _node_server_args(server: McpServerConfig) -> list[str]:
    """Server args that FOLLOW the package spec — the npx flags before it (``-y`` /
    ``--silent``) are npx-only and dropped for a direct ``node`` launch."""
    pkg = _npx_package(server)
    if pkg is None or pkg not in server.args:
        return []
    return list(server.args[server.args.index(pkg) + 1:])


def _url_ok(url: str, proxy: str | None = None, timeout: float = 3.0) -> bool:
    """True if a quick HEAD to ``url`` (optionally through ``proxy``) gets ANY HTTP
    response; only a connection failure / timeout counts as unreachable. Unlike a
    raw TCP probe this honors the proxy and is bounded by a single timeout."""
    import urllib.error
    import urllib.request

    opener = urllib.request.build_opener(
        urllib.request.ProxyHandler({"http": proxy, "https": proxy} if proxy else {})
    )
    try:
        with opener.open(urllib.request.Request(url, method="HEAD"), timeout=timeout):
            return True
    except urllib.error.HTTPError:
        return True
    except (urllib.error.URLError, OSError):
        return False


def _discover_proxy(config: "McpConfig | None") -> str | None:
    """The proxy to try for package fetches: an explicit ``mcp.proxy`` if set, else
    the system proxy (env vars + Windows/macOS settings, via ``getproxies()``)."""
    import urllib.request

    if config is not None and getattr(config, "proxy", None):
        return config.proxy
    proxies = urllib.request.getproxies()
    return proxies.get("https") or proxies.get("http")


def _pick_source(values, proxy, probe) -> tuple[str, str | None] | None:
    """First reachable ``(url, proxy_or_none)`` among ``values`` (an ordered list of
    candidate registry/index URLs: override, official, mirror). Each is tried
    directly first, then through ``proxy``. ``probe(url, proxy) -> bool`` is the
    reachability test (injected so tests need no network)."""
    for url in values:
        if not url:
            continue
        if probe(url, None):
            return url, None
        if proxy and probe(url, proxy):
            return url, proxy
    return None


def _apply_proxy(out: dict[str, str], proxy: str) -> None:
    out["HTTP_PROXY"] = out["HTTPS_PROXY"] = proxy
    out["http_proxy"] = out["https_proxy"] = proxy
    out["npm_config_proxy"] = out["npm_config_https-proxy"] = proxy


def _stdio_net_env(servers: list[McpServerConfig], config: "McpConfig | None" = None) -> dict[str, str]:
    """Network env injected into stdio children so ``npx``/``uvx`` can fetch their
    package on ANY network: pick a reachable registry/index (an explicit override,
    else the official source, else a domestic mirror), tried directly or through the
    system/configured proxy. NEVER overrides a registry/index/proxy the user set.
    Probes the two package managers concurrently (startup waits on the slower one)."""
    import concurrent.futures
    import os

    cmds = {_base_command(s.command) for s in servers if s.kind == "stdio"}
    proxy = _discover_proxy(config)
    npm_override = getattr(config, "npm_registry", None)
    pip_override = getattr(config, "pip_index", None)

    def _npm():
        if not ({"npx", "npm", "node"} & cmds):
            return None
        if os.environ.get("npm_config_registry") or os.environ.get("NPM_CONFIG_REGISTRY"):
            return None
        return _pick_source([npm_override, _NPM_OFFICIAL, _NPM_MIRROR], proxy, _url_ok)

    def _pip():
        if not ({"uvx", "uv"} & cmds):
            return None
        if os.environ.get("UV_DEFAULT_INDEX") or os.environ.get("UV_INDEX"):
            return None
        return _pick_source([pip_override, _PYPI_OFFICIAL, _PYPI_MIRROR], proxy, _url_ok)

    with concurrent.futures.ThreadPoolExecutor(max_workers=2) as pool:
        npm_fut, pip_fut = pool.submit(_npm), pool.submit(_pip)
        npm_res, pip_res = npm_fut.result(), pip_fut.result()

    out: dict[str, str] = {}
    if npm_res:
        reg, prox = npm_res
        out["npm_config_registry"] = reg
        out["npm_config_fetch_retries"] = "2"
        if prox:
            _apply_proxy(out, prox)
    if pip_res:
        idx, prox = pip_res
        out["UV_DEFAULT_INDEX"] = idx
        if prox:
            _apply_proxy(out, prox)
    return out


def _source_status(server: McpServerConfig, net_env: dict[str, str]) -> tuple[bool, str]:
    """``(source_configured, family)`` for a stdio server's package fetch: whether a
    registry/index is set (user env or the resolved ``net_env``) and which family
    (``npm``/``uv``); ``("" family)`` for launchers that don't fetch a package."""
    import os

    cmd = _base_command(server.command)
    if cmd in {"npx", "npm", "node"}:
        configured = bool(
            os.environ.get("npm_config_registry")
            or os.environ.get("NPM_CONFIG_REGISTRY")
            or net_env.get("npm_config_registry")
        )
        return configured, "npm"
    if cmd in {"uvx", "uv"}:
        configured = bool(
            os.environ.get("UV_DEFAULT_INDEX")
            or os.environ.get("UV_INDEX")
            or net_env.get("UV_DEFAULT_INDEX")
        )
        return configured, "uv"
    return True, ""


def _unreachable_advice(family: str) -> str:
    where = "an npm registry" if family == "npm" else "a PyPI index"
    knob = "mcp.npm_registry" if family == "npm" else "mcp.pip_index"
    return (
        f"couldn't reach {where} (official or mirror, directly or via proxy). Set "
        f"mcp.proxy or {knob} in config.yaml, pre-install the package, or remove "
        "this server."
    )


def _connect_error(server: McpServerConfig, exc: BaseException, net_env: dict[str, str]) -> str:
    """A connect failure as a readable message: drill the ExceptionGroup leaf, then
    for a stdio npx/uvx server add WHY a package fetch likely failed."""
    msg = _format_exception(exc)
    if server.kind != "stdio":
        return msg
    configured, family = _source_status(server, net_env)
    if family and not configured:
        return f"{msg} — {_unreachable_advice(family)}"
    if family and "timeout" in msg.lower():
        return (
            f"{msg} — the package may still be cold-downloading; run `mcp warm` "
            "first, or raise mcp.connect_timeout_seconds in config.yaml. If the "
            "package is already prepared/ready, a handshake timeout usually means "
            "the server wrote non-JSON-RPC text to its stdout (a server-side bug) — "
            "check its logs or try a different server."
        )
    return msg


def _open_streams(server: McpServerConfig, net_env: dict[str, str] | None = None, errlog=None):
    """Return the (read, write) stream context manager for ``server``'s transport.

    Transport is ``server.kind`` (explicit ``transport`` or inferred from shape):
    ``stdio`` launches a subprocess; ``http`` is the modern Streamable HTTP;
    ``sse`` is the legacy HTTP+SSE transport for older remote servers. ``errlog``
    (a writable file) captures the child's stderr — the npx/uvx fetch progress the
    management page surfaces while a server is connecting.
    """
    from .config import resolve_env

    kind = server.kind
    if kind == "stdio":
        import os

        from mcp import StdioServerParameters
        from mcp.client.stdio import stdio_client

        env = dict(os.environ)
        # `env_const` is AUTHORITATIVE: it OVERRIDES an ambient same-named var in
        # the parent process. These are per-server REQUIRED literals from config.yaml
        # (e.g. MODE=stdio, which forces open-websearch into pure-stdio) — and the
        # names can be generic (`MODE`!), so a stray value inherited from the shell
        # that launched the harness must NOT win, or the server breaks (open-websearch
        # with MODE=web starts no transport and exits -> "Connection closed"). To
        # change it, edit config.yaml's env_const, not an ambient env var.
        for key, value in server.env_const.items():
            env[key] = value
        for name in server.env:  # inject named secrets from .env into the child
            value = resolve_env(name)
            if value is not None:
                env[name] = value
        for key, value in (net_env or {}).items():  # resolved registry/proxy; user's setting wins
            env.setdefault(key, value)
        # Node (npx) servers launch DIRECTLY from their stable per-server install
        # (`node <bin>`) — never the ephemeral npx cache (see `_node_install_dir`).
        if _npx_package(server) is not None:
            command, args = _windows_stdio_command(
                "node", [_node_bin_path(server), *_node_server_args(server)]
            )
        else:
            command, args = _windows_stdio_command(server.command, server.args)
        params = StdioServerParameters(command=command, args=args, env=env)
        return stdio_client(params, errlog) if errlog is not None else stdio_client(params)

    headers = _bearer_headers(server)
    if kind == "sse":  # legacy HTTP+SSE transport (older servers)
        from mcp.client.sse import sse_client

        return sse_client(server.url, headers=headers)

    from mcp.client import streamable_http as sh

    if hasattr(sh, "streamablehttp_client"):  # SDK <2.x
        return sh.streamablehttp_client(server.url, headers=headers)
    import httpx  # SDK >=2.x: headers go on a pre-built client

    return sh.streamable_http_client(server.url, http_client=httpx.AsyncClient(headers=headers))


def _result_text(result: Any) -> str:
    """Flatten an MCP CallToolResult's content blocks into a plain string."""
    parts = [getattr(b, "text", "") for b in (getattr(result, "content", None) or [])]
    text = "\n".join(p for p in parts if p)
    return f"ERROR: {text}" if getattr(result, "isError", False) else text


def _format_exception(exc: BaseException) -> str:
    """Show useful leaves from Python 3.11 ExceptionGroup/anyio TaskGroup errors.

    A stdio spawn failure / connect timeout surfaces from the MCP SDK as 'unhandled
    errors in a TaskGroup' — noise; the leaf (e.g. ``FileNotFoundError: [WinError 2]
    ...``) is the actionable part shown in the panel.
    """
    if isinstance(exc, BaseExceptionGroup):
        leaves: list[str] = []

        def _walk(group: BaseExceptionGroup) -> None:
            for inner in group.exceptions:
                if isinstance(inner, BaseExceptionGroup):
                    _walk(inner)
                else:
                    message = str(inner) or type(inner).__name__
                    leaves.append(f"{type(inner).__name__}: {message}")

        _walk(exc)
        if leaves:
            return f"{exc} ({'; '.join(leaves)})"
    return str(exc) or type(exc).__name__


def _exc_has(exc: BaseException, exc_type: type[BaseException]) -> bool:
    """True if ``exc`` IS ``exc_type`` or contains it as an (anyio TaskGroup)
    ExceptionGroup leaf — e.g. the ``FileNotFoundError`` a missing npx/uvx surfaces
    (bare on Windows, wrapped in a TaskGroup on POSIX)."""
    if isinstance(exc, exc_type):
        return True
    if isinstance(exc, BaseExceptionGroup):
        return any(_exc_has(inner, exc_type) for inner in exc.exceptions)
    return False


class McpManager:
    """Owns one background event loop + the full MCP session lifecycle.

    Each configured server gets its OWN task that connects CONCURRENTLY and then
    owns that server's session AND its tool-call execution for the whole run —
    anyio requires a session be entered, used, and exited in the SAME task, so the
    dispatcher only routes a call to the owning task. Per-server failure is isolated
    into ``errors`` (a server that won't start is skipped, not fatal). ``begin()``
    starts connecting WITHOUT blocking so ``status()`` can report a live
    ``connecting`` state; ``start()`` also waits for the first round to settle (for
    a one-shot CLI run that needs tools up front). ``reconnect_server()`` restarts
    one server in place; ``call()`` is synchronous + thread-safe; ``close()`` shuts
    everything down cleanly.

    Connecting is TWO phases (see :meth:`_run_server`): a survivable package PREFETCH
    (npx/uvx) then a bounded HANDSHAKE; a failed connect auto-heals in the background
    with backoff, and ``on_connected`` (called with ``self`` on every successful
    connect) lets the owner re-sync its tool registry so healed tools reach the model.
    """

    _BACKOFF_BASE = 5.0  # auto-heal backoff: doubles each attempt …
    _BACKOFF_CAP = 60.0  # … capped here
    _HEAL_DEADLINE_SECONDS = 300.0  # total auto-heal budget per failure streak (bounds a hanging server)

    def __init__(self, config: McpConfig, on_connected=None) -> None:
        self._config = config
        self._loop = asyncio.new_event_loop()
        self._thread = threading.Thread(target=self._thread_main, daemon=True)
        self._ready: concurrent.futures.Future = concurrent.futures.Future()
        self._inbox: asyncio.Queue | None = None
        self._closing: asyncio.Event | None = None
        self._net_env: dict[str, str] = {}
        self._servers: dict[str, McpServerConfig] = {s.name: s for s in config.servers}
        self._tasks: dict[str, asyncio.Task] = {}
        self._settled: dict[str, asyncio.Event] = {}
        self._server_inboxes: dict[str, asyncio.Queue] = {}
        self._sessions: dict[str, Any] = {}
        self._connect_started: dict[str, float] = {}  # monotonic start, for live elapsed
        self._logpaths: dict[str, str] = {}  # per-server stderr capture (npx/uvx fetch log)
        self.discovered: dict[str, list] = {}
        self.errors: dict[str, str] = {}
        self._on_connected = on_connected  # called(self) after each successful connect
        self._notes: dict[str, str] = {}  # transient per-server status line (e.g. retrying)

    def _thread_main(self) -> None:
        asyncio.set_event_loop(self._loop)
        self._net_env = _stdio_net_env(list(self._config.servers), self._config)  # probe off the loop
        if self._net_env:
            # WHICH registry/mirror (+ whether a proxy) the auto-probe picked — the
            # answer to "why is npx/uvx slow or failing to fetch its package". The
            # registry/index URLs are not secrets (config calls them "values only");
            # a proxy URL can embed credentials, so only note that one is in effect.
            picked = {
                k: v for k, v in self._net_env.items()
                if k in ("npm_config_registry", "UV_DEFAULT_INDEX")
            }
            if any("proxy" in k.lower() for k in self._net_env):
                picked["proxy"] = "in_use"
            log.debug("mcp package-fetch resolved: %s", picked)
        try:
            self._loop.run_until_complete(self._main())
        except Exception as exc:  # surface startup failures to start()
            if not self._ready.done():
                self._ready.set_exception(exc)
        finally:
            self._loop.close()

    async def _main(self) -> None:
        self._inbox = asyncio.Queue()
        self._closing = asyncio.Event()
        for server in self._config.servers:
            self._spawn(server)
        # Ready once every server's first connect attempt has settled (connected or
        # failed) — start()/wait_ready() block on this; begin() does not.
        await asyncio.gather(*(ev.wait() for ev in list(self._settled.values())))
        self._ready.set_result(True)
        await self._serve()
        self._closing.set()
        for inbox in list(self._server_inboxes.values()):  # stop each server's loop
            await inbox.put(None)
        if self._tasks:
            await asyncio.gather(*self._tasks.values(), return_exceptions=True)

    def _spawn(self, server: McpServerConfig) -> None:
        """Create one server's settled event + its lifecycle task (on the loop)."""
        name = server.name
        self._servers[name] = server
        self._sessions.pop(name, None)
        self.errors.pop(name, None)
        self.discovered.pop(name, None)
        self._server_inboxes.pop(name, None)
        self._connect_started.pop(name, None)
        self._settled[name] = asyncio.Event()
        self._tasks[name] = asyncio.create_task(self._run_server(server))

    async def _prefetch(self, server: McpServerConfig, errlog) -> None:
        """Phase 1 (npx/uvx only): pre-download the package via a SURVIVABLE
        subprocess, off the event loop, with a generous budget and progress streamed
        to ``errlog`` (so the status page shows it). This is what makes a slow cold
        download finish ONCE — the short handshake timeout would otherwise kill it,
        and on Windows that tears down the whole download tree, losing all progress
        every attempt. No-op for non-package servers; never fatal (a fetch hiccup
        just falls through to the handshake, which then errors + auto-heals)."""
        if _warm_argv(server) is None:
            return
        warm_timeout = max(120.0, self._config.connect_timeout_seconds)

        def _blocking() -> None:
            outcome = _warm_one_server(server, self._net_env, errlog, warm_timeout=warm_timeout)
            if outcome is not None and errlog is not None:
                ok, detail = outcome
                errlog.write(f"[{server.name}] {'ready' if ok else 'fetch: ' + detail}\n")
                errlog.flush()

        try:
            await self._loop.run_in_executor(None, _blocking)
        except Exception:  # never let a prefetch glitch abort the handshake
            log.debug("mcp %s: prefetch error (continuing to handshake)", server.name, exc_info=True)

    def _fire_connected(self) -> None:
        """Notify the owner a server (re)connected, so it can re-sync its tool
        registry — without this, a server that auto-heals after the initial round
        would be connected but its tools never offered to the model."""
        if self._on_connected is None:
            return
        try:
            self._on_connected(self)
        except Exception:
            log.debug("mcp on_connected callback failed", exc_info=True)

    async def _run_server(self, server: McpServerConfig) -> None:
        """Own ONE server's session AND its tool calls for the whole run, in a
        single task — connecting in TWO phases, with silent auto-heal.

        Phase 1 PREFETCH (see :meth:`_prefetch`) downloads an npx/uvx package once in
        a survivable subprocess; phase 2 opens the transport + ``initialize`` +
        ``list_tools`` under ``connect_timeout_seconds`` (fast once cached). Each
        ``call_tool`` runs HERE — never from the dispatcher task, since anyio requires
        a session be used in the same task it was entered. A ``None`` on the inbox
        stops the loop (shutdown or reconnect).

        A connect failure is isolated AND retried in the background with exponential
        backoff (up to ``connect_max_retries``, but never longer than
        ``_HEAL_DEADLINE_SECONDS`` total): the dot stays amber while retrying and goes
        red only once retries are exhausted, so a server that was merely slow or
        briefly unreachable heals on its own. A MISSING launcher (no Node/uv) is not
        retried — retrying can't conjure it, so it goes red at once. Every successful
        connect re-syncs the registry via :meth:`_fire_connected`.
        """
        name = server.name
        settled = self._settled[name]
        attempt = 0
        heal_started: float | None = None  # monotonic of the first failure in a streak
        while True:
            self._notes.pop(name, None)
            self._connect_started[name] = time.monotonic()
            errlog = self._open_log(name)
            try:
                async with AsyncExitStack() as stack:
                    if errlog is not None:
                        stack.callback(self._close_log, name, errlog)
                    await self._prefetch(server, errlog)  # phase 1 (no-op for non-package)
                    if errlog is not None:  # supersede the prefetch's "ready" so status shows the live phase
                        errlog.write(f"[{name}] connecting (MCP handshake)...\n")
                        errlog.flush()
                    # `asyncio.wait_for(coro)` wraps `coro` in another Task; anyio's MCP
                    # transports must be entered and exited in the SAME task, so use
                    # `asyncio.timeout()` here instead.
                    async with asyncio.timeout(self._config.connect_timeout_seconds):
                        await self._connect(stack, server, errlog)  # phase 2
                    inbox: asyncio.Queue = asyncio.Queue()
                    self._server_inboxes[name] = inbox
                    self.errors.pop(name, None)  # connected (or healed)
                    attempt = 0  # a clean connection resets the backoff
                    log.debug(
                        "mcp %s: connected in %.1fs (%d tools)",
                        name, time.monotonic() - self._connect_started[name],
                        len(self.discovered.get(name, [])),
                    )
                    settled.set()
                    self._fire_connected()  # re-sync the registry so healed tools reach the model
                    while True:
                        req = await inbox.get()
                        if req is None:
                            return  # shutdown or reconnect: tear the session down in this task
                        tool, arguments, fut = req
                        try:
                            # Bound each call: a hung server fails this ONE call with a
                            # timeout ERROR the model can react to, instead of wedging
                            # the turn (and Stop) forever.
                            async with asyncio.timeout(self._config.call_timeout_seconds):
                                fut.set_result(
                                    _result_text(await self._sessions[name].call_tool(tool, arguments))
                                )
                        except TimeoutError:
                            fut.set_exception(TimeoutError(
                                f"MCP tool {name}__{tool} timed out after "
                                f"{self._config.call_timeout_seconds:.0f}s "
                                "(mcp.call_timeout_seconds in config.yaml)"
                            ))
                        except Exception as exc:
                            fut.set_exception(exc)
            except asyncio.CancelledError:
                raise  # reconnect/shutdown cancelled us — don't auto-heal, just exit
            except Exception as exc:  # connect failure/timeout: isolate, never fatal
                self._sessions.pop(name, None)
                self._server_inboxes.pop(name, None)
                detail = _connect_error(server, exc, self._net_env)
                attempt += 1
                if heal_started is None:
                    heal_started = time.monotonic()
                if not settled.is_set():
                    settled.set()  # let start()/wait_ready() proceed after the first attempt
                # Stop retrying if: the launcher is MISSING (no Node/uv — retrying can't
                # fix it), retries are exhausted, the total heal budget elapsed (bounds a
                # server that hangs the handshake every attempt), or we're shutting down.
                missing_launcher = _exc_has(exc, FileNotFoundError)
                expired = time.monotonic() - heal_started > self._HEAL_DEADLINE_SECONDS
                if (missing_launcher or expired or self._closing.is_set()
                        or attempt > self._config.connect_max_retries):
                    self.errors[name] = detail  # give up: red, until a manual reconnect
                    why = ("missing launcher" if missing_launcher else
                           "heal deadline" if expired else f"gave up after {attempt}")
                    log.debug("mcp %s: connect FAILED (%s): %s", name, why, detail)
                    return
                # Auto-heal: stay amber (no error set) and retry after a backoff.
                self.errors.pop(name, None)
                delay = min(self._BACKOFF_BASE * (2 ** (attempt - 1)), self._BACKOFF_CAP)
                self._notes[name] = f"connect failed; retrying in {int(delay)}s ({detail[:80]})"
                log.debug("mcp %s: connect failed (attempt %d), retrying in %.0fs: %s", name, attempt, delay, detail)
                try:
                    await asyncio.wait_for(self._closing.wait(), timeout=delay)
                    return  # shutting down during backoff
                except TimeoutError:
                    pass  # backoff elapsed → loop and retry

    def _open_log(self, name: str):
        """A per-server file that captures the child's stderr (npx/uvx fetch output);
        ``status()`` tails it while connecting. Best-effort: ``None`` on failure (the
        connect then just uses the default stderr — never blocked by logging)."""
        import os
        import tempfile

        try:
            path = os.path.join(tempfile.gettempdir(), f"mcp-{name}-{id(self):x}.log")
            self._logpaths[name] = path
            return open(path, "w", encoding="utf-8", buffering=1)
        except OSError:
            self._logpaths.pop(name, None)
            return None

    def _close_log(self, name: str, handle) -> None:
        import contextlib
        import os

        with contextlib.suppress(Exception):
            handle.close()
        path = self._logpaths.pop(name, None)
        if path:
            with contextlib.suppress(OSError):
                os.unlink(path)

    def _log_tail(self, name: str) -> str:
        """The last meaningful stderr line of ``name`` (npx/uvx progress), or ''."""
        path = self._logpaths.get(name)
        if not path:
            return ""
        try:
            with open(path, encoding="utf-8", errors="replace") as fh:
                lines = [ln.strip() for ln in fh.read().splitlines() if ln.strip()]
        except OSError:
            return ""
        return lines[-1] if lines else ""

    async def _connect(self, stack: AsyncExitStack, server: McpServerConfig, errlog=None) -> None:
        """Open one server's session and list its tools (under a startup timeout)."""
        from mcp import ClientSession

        streams = await stack.enter_async_context(_open_streams(server, self._net_env, errlog))
        session = await stack.enter_async_context(ClientSession(streams[0], streams[1]))
        await session.initialize()
        self._sessions[server.name] = session
        self.discovered[server.name] = list((await session.list_tools()).tools)

    async def _serve(self) -> None:
        """Dispatcher: route each synchronous ``call()`` to its server's own task."""
        assert self._inbox is not None
        while True:
            req = await self._inbox.get()
            if req is None:
                return
            server, tool, arguments, fut = req
            inbox = self._server_inboxes.get(server)
            if inbox is None:
                # The model tried a tool on a server that never connected — leave a
                # trail (the connect error / 'not discovered') so the failure isn't
                # invisible behind a generic tool ERROR.
                log.debug(
                    "mcp %s: tool call rejected, server not connected (%s)",
                    server, self.errors.get(server) or "not discovered",
                )
                fut.set_exception(
                    RuntimeError(
                        f"MCP server {server!r} is not connected: "
                        f"{self.errors.get(server) or 'not discovered'}"
                    )
                )
                continue
            await inbox.put((tool, arguments, fut))

    async def _restart(self, name: str) -> None:
        """Tear down one server (in its own task) and spawn a fresh one — in place."""
        server = self._servers.get(name)
        if server is None:
            return
        log.debug("mcp %s: restarting (reconnect requested)", name)
        task = self._tasks.get(name)
        if task is not None and not task.done():
            # Drop the inbox first so the dispatcher fails fast (instead of routing
            # to a loop that's about to exit) for the brief reconnect window.
            inbox = self._server_inboxes.pop(name, None)
            if inbox is not None:
                await inbox.put(None)  # graceful: loop returns, session torn down in its task
            else:
                task.cancel()  # still connecting (no inbox yet)
            try:
                await task
            except (asyncio.CancelledError, Exception):
                pass
        self._spawn(server)

    def begin(self) -> None:
        """Start connecting WITHOUT blocking (``status()`` then reports live progress)."""
        if not self._thread.is_alive():
            self._thread.start()

    def start(self) -> None:
        """Start and block until the first connect round settles (CLI one-shot)."""
        self.begin()
        self._ready.result()  # block until every server is connected (or skipped)

    def wait_ready(self, timeout: float | None = None) -> None:
        """Block until the initial connect round has settled (connected or failed)."""
        self._ready.result(timeout)

    def wait_server_ready(self, name: str, timeout: float | None = None) -> None:
        """Block until ONE server's current attempt settles (after a reconnect)."""
        ev = self._settled.get(name)
        if ev is None or self._loop.is_closed():
            return
        try:
            asyncio.run_coroutine_threadsafe(ev.wait(), self._loop).result(timeout)
        except (concurrent.futures.TimeoutError, RuntimeError):
            pass

    def reconnect_server(self, name: str) -> None:
        """Restart ONE server in place (others untouched); returns once it's been
        re-spawned and is (re)connecting — poll ``status()`` for the outcome."""
        if name not in self._servers or self._loop.is_closed():
            return
        asyncio.run_coroutine_threadsafe(self._restart(name), self._loop).result()

    def call(self, server: str, tool: str, arguments: dict) -> str:
        fut: concurrent.futures.Future = concurrent.futures.Future()
        self._loop.call_soon_threadsafe(self._inbox.put_nowait, (server, tool, arguments, fut))
        # The server task bounds the call (call_timeout_seconds); the grace here
        # only covers a wedged event loop, so call() can never hang its caller.
        try:
            return fut.result(timeout=self._config.call_timeout_seconds + 10)
        except TimeoutError:
            # concurrent.futures.TimeoutError IS builtin TimeoutError (3.11+), so a
            # done future means the server task's own timeout — keep its message.
            if fut.done():
                raise
            raise TimeoutError(
                f"MCP tool {server}__{tool} timed out (manager unresponsive)"
            ) from None

    def close(self) -> None:
        if self._loop.is_closed() or self._inbox is None:
            return
        self._loop.call_soon_threadsafe(self._inbox.put_nowait, None)
        self._thread.join(timeout=10)

    def status(self) -> list[dict]:
        """Per-server health — the source of truth for the management page /
        ``mcp status``: each configured server's transport, live connection state
        (``connected`` / ``connecting`` / errored), discovered tool count, and
        connect error (if any). ``connecting`` is true while a server's first (or a
        reconnect's) attempt is still in flight — the UI shows it amber, not red."""
        out: list[dict] = []
        for server in self._config.servers:
            name = server.name
            tools = self.discovered.get(name)
            connected = name in self._sessions
            error = self.errors.get(name)
            connecting = self._thread.is_alive() and not connected and error is None
            started = self._connect_started.get(name)
            out.append(
                {
                    "name": name,
                    "transport": server.kind,
                    "connected": connected,
                    "connecting": connecting,
                    # Live feedback while a (possibly slow) connect/fetch is in flight:
                    # how long it's been trying + the latest stderr line (npx/uvx output).
                    "connecting_seconds": round(time.monotonic() - started, 1) if (connecting and started) else None,
                    # The live auto-heal note (retrying/backoff) wins over the captured
                    # stderr tail, so a stale "ready" prefetch line never masks a server
                    # that's actually still handshaking / retrying.
                    "log_tail": (self._notes.get(name) or self._log_tail(name) or "") if connecting else "",
                    "error": error,
                    "tool_count": len(tools) if tools is not None else 0,
                }
            )
        return out


def rebuild_manager(
    old: "McpManager | None", config: McpConfig, on_connected=None
) -> McpManager:
    """Reconnect: start a fresh manager for ``config``, then close the old one.

    The new manager is fully started (every server connected or failure-isolated)
    BEFORE the old one is closed, so a hot-reconnect never leaves a window with no
    sessions. The caller keeps the returned manager and re-syncs the registry.
    ``on_connected`` carries over to the fresh manager so its servers auto-heal +
    re-sync just like the original (see :class:`McpManager`).
    """
    manager = McpManager(config, on_connected=on_connected)
    manager.start()
    if old is not None:
        old.close()
    return manager


def _warm_argv(server: McpServerConfig) -> tuple[str, list[str]] | None:
    """The 'prepare the package, then exit' command for an npx/uvx server (so warming
    doesn't start the long-lived server), or ``None`` when the launcher isn't a
    package fetch. ``npx`` -> ``npm install`` into a stable per-server prefix (the
    launch then runs ``node <bin>`` from it, never the ephemeral npx cache); ``uvx``
    -> resolve the tool env then run a no-op ``python``."""
    cmd = _base_command(server.command)
    if cmd == "npx":
        return _node_install_argv(server)
    if cmd == "uvx":
        pkg = next((a for a in server.args if not a.startswith("-")), None)
        return (server.command, ["--from", pkg, "python", "-c", ""]) if pkg else None
    return None


def _stream_subprocess(argv, env, timeout, on_line, heartbeat_every: float = 5.0):
    """Run ``argv``, handing each output line to ``on_line`` AS IT ARRIVES, and
    emit a heartbeat line if the child is quiet for ``heartbeat_every`` seconds so a
    long, silent cold download never looks frozen. Returns ``(returncode, timed_out)``
    (``returncode`` is ``None`` when killed at ``timeout``). Cross-platform: a reader
    thread feeds a queue (no ``select`` on pipes, which Windows lacks)."""
    import queue
    import subprocess
    import threading

    proc = subprocess.Popen(  # noqa: S603 — config-defined launcher, fixed argv
        list(argv),
        stdin=subprocess.DEVNULL,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        env=env,
        text=True,
        bufsize=1,
    )
    lines: queue.Queue = queue.Queue()

    def _reader() -> None:
        try:
            for line in proc.stdout:  # type: ignore[union-attr]
                lines.put(line.rstrip("\n"))
        finally:
            lines.put(None)  # stream closed

    threading.Thread(target=_reader, daemon=True).start()
    start = time.monotonic()
    while True:
        try:
            line = lines.get(timeout=heartbeat_every)
        except queue.Empty:
            on_line(f"... still downloading ({int(time.monotonic() - start)}s)")
        else:
            if line is None:
                break
            on_line(line)
        if time.monotonic() - start > timeout:
            proc.kill()
            return None, True
    proc.wait()
    return proc.returncode, False


def _warm_one_server(
    server: McpServerConfig, net_env: dict[str, str], log, *, warm_timeout: float
) -> tuple[bool, str] | None:
    """Pre-download ONE npx/uvx server's package, streaming progress to ``log``.

    Returns ``(ok, detail)`` or ``None`` when there's nothing to fetch (a non-npx/uvx
    launcher). Best-effort (never raises): an unreachable registry / missing launcher
    / non-zero exit each come back as ``(False, reason)``; a timeout is ``(True, …)``
    because the (survivable) download keeps going and finishes for the next attempt.
    Shared by :func:`prewarm_stdio_packages` and ``McpManager._prefetch``."""
    import os

    warm = _warm_argv(server)
    if warm is None:  # a local script / non-npx-uvx launcher — nothing to fetch
        return None
    # A Node server whose pinned version is already installed needs nothing: skip the
    # `npm install` reconcile so every later connect is fast AND works fully offline.
    if _npx_package(server) is not None and _node_satisfied(server):
        return True, "ready (cached)"
    configured, family = _source_status(server, net_env)
    if not configured:  # no reachable registry found — don't hang, report it
        return False, _unreachable_advice(family)
    try:
        command, args = _windows_stdio_command(warm[0], warm[1])
    except FileNotFoundError as exc:
        return False, str(exc)
    env = dict(os.environ)
    for key, value in net_env.items():
        env.setdefault(key, value)
    if _npx_package(server) is not None:  # a Node (npm) install: skip blocked browser-binary postinstalls
        for key, value in _NODE_INSTALL_SKIP_DOWNLOAD.items():
            env.setdefault(key, value)

    def _say(msg: str) -> None:
        if log is not None:
            log.write(msg + "\n")
            log.flush()

    _say(f"[{server.name}] preparing: {server.command} {' '.join(server.args)} ...")
    tail: list[str] = []

    def _on_line(line: str) -> None:
        tail.append(line)
        del tail[:-5]  # keep only the last few lines for the failure detail
        _say(f"  [{server.name}] {line}")

    try:
        code, timed_out = _stream_subprocess([command, *args], env, warm_timeout, _on_line)
    except Exception as exc:  # spawn failure (missing launcher, etc.)
        return False, _format_exception(exc)
    if timed_out:
        return True, f"still fetching after {int(warm_timeout)}s (will finish on first connect)"
    if code == 0:
        return True, "ready"
    # `npm install` can exit non-zero from its post-install cache CLEANUP alone
    # (Windows `EPERM: rmdir` under antivirus / file locks) while the package
    # actually installed fine — trust the installed tree, not the exit code.
    if family == "npm" and _node_satisfied(server):
        return True, "ready (npm cleanup warning ignored)"
    return False, f"launcher exited {code}: {'; '.join(tail[-3:])}".strip()


def prewarm_stdio_packages(
    config: "McpConfig", *, log=None, timeout: float | None = None
) -> list[tuple[str, bool, str]]:
    """Pre-download each npx/uvx server's package (into the launcher's cache) using
    the resolved registry/proxy, so the FIRST real connect is fast and an
    unreachable registry surfaces here (a readable error) instead of later as a
    connect ``TimeoutError``. Fetch-only (a no-op command after install), so it
    exits promptly without starting the server. Returns ``(name, ok, detail)`` per
    server; best-effort (never raises).
    """
    net_env = _stdio_net_env(list(config.servers), config)
    warm_timeout = timeout or max(120.0, config.connect_timeout_seconds)
    results: list[tuple[str, bool, str]] = []
    for server in config.servers:
        if server.kind != "stdio":
            continue
        outcome = _warm_one_server(server, net_env, log, warm_timeout=warm_timeout)
        if outcome is None:  # nothing to fetch for this launcher
            continue
        ok, detail = outcome
        results.append((server.name, ok, detail))
        if log is not None:
            log.write(f"[{server.name}] {'ok' if ok else 'FAILED'}: {detail}\n")
            log.flush()
    return results


def _warm_signature(config: "McpConfig") -> str:
    """A stable fingerprint of the stdio package-fetch servers (name + launcher +
    args, which includes the pinned version), so a one-time warm can be skipped on
    later launches and re-runs only when a launcher / version actually changes."""
    import hashlib
    import json

    items = sorted(
        [server.name, server.command or "", list(server.args)]
        for server in config.servers
        if server.kind == "stdio" and _warm_argv(server) is not None
    )
    blob = json.dumps(items, sort_keys=True, ensure_ascii=False)
    return hashlib.sha256(blob.encode("utf-8")).hexdigest()


def warm_once(
    config: "McpConfig", *, log=None, force: bool = False, state_dir: str = ".harness"
) -> list[tuple[str, bool, str]] | None:
    """Warm stdio packages ONCE per launcher/version set, recording a sentinel so
    every later launch skips straight to a (now fast) connect — the first run pays
    the cold download, with live progress, and nothing after it does.

    Returns the per-server results, or ``None`` when there is nothing to warm or the
    set was already warmed. Best-effort: a sentinel read/write error never blocks
    warming, and warming itself never raises (see :func:`prewarm_stdio_packages`).

    The sentinel is written even when warming reports a FAILURE (e.g. no Node, or an
    unreachable registry while the package is already installed locally): re-running a
    foreground warm every launch can't fix a permanent gap and only nags, while a
    transient miss is still recovered by the connect-time prefetch + auto-heal. So
    warm-once means once, not once-per-launch-until-everything-succeeds."""
    import os

    if not any(s.kind == "stdio" and _warm_argv(s) is not None for s in config.servers):
        return None
    signature = _warm_signature(config)
    sentinel = os.path.join(state_dir, ".mcp-warmed")
    if not force:
        try:
            with open(sentinel, encoding="utf-8") as fh:
                if fh.read().strip() == signature:
                    return None  # this exact set is already in the cache
        except OSError:
            pass
    if log is not None:
        log.write(
            "[mcp] preparing tool packages (one-time; the first run can take a "
            "minute on a slow network) ...\n"
        )
        log.flush()
    results = prewarm_stdio_packages(config, log=log)
    if results:  # warm ran (success OR failure) → record it; don't re-nag every launch
        try:
            os.makedirs(state_dir, exist_ok=True)
            with open(sentinel, "w", encoding="utf-8") as fh:
                fh.write(signature)
        except OSError:
            pass
    return results


def sync_mcp_tools(
    config: "Config", manager: McpManager, registry: Registry | None = None
) -> list[str]:
    """Re-sync the registry's MCP tools to ``config`` + ``manager.discovered``.

    Unlike :func:`register_mcp_tools` (append-only, for a one-shot CLI run), this
    first DROPS every existing ``<server>__<tool>`` entry, then re-registers the
    currently-allowlisted discovered tools — so a long-lived process (the web
    server) stays consistent across runtime allowlist/server edits: what's
    registered (hence offered to the model) always matches the panel's view.
    """
    registry = registry or default_registry
    registry.remove_where(lambda name: "__" in name)
    return register_mcp_tools(config, manager, registry)


def register_mcp_tools(
    config: "Config", manager: McpManager, registry: Registry | None = None
) -> list[str]:
    """Register allowlisted MCP tools into ``registry`` (default: the shared one).

    Only tools allowlisted in ``config.tools`` are registered — by exact
    ``{server}__{tool}`` name or by a ``{server}__*`` wildcard that enables the
    server's whole toolset (see :func:`~{{ project_slug }}.harness.tools.in_allowlist`);
    a non-allowlisted MCP tool is never connected to the model. Risk is per-tool: a
    tool listed in its server's ``safe_tools`` registers as ``risk=safe`` (read-only;
    offered to plan/ask); every other tool defaults to ``risk=high`` (mutating/
    external; agent-only, opt in deliberately).
    """
    from .tools import in_allowlist

    registry = registry or default_registry
    allowed = config.enabled_tool_names()
    safe_by_server = {s.name: set(s.safe_tools) for s in config.mcp.servers}
    registered: list[str] = []
    for server, tools in manager.discovered.items():
        safe = safe_by_server.get(server, set())
        for tool in tools:
            name = f"{server}__{tool.name}"
            if not in_allowlist(name, allowed):
                continue
            registry.register(
                Tool(
                    name=name,
                    description=tool.description or "",
                    parameters=tool.inputSchema or {"type": "object", "properties": {}},
                    func=_make_caller(manager, server, tool.name),
                    risk=SAFE if tool.name in safe else HIGH,
                )
            )
            registered.append(name)
    return registered


def _make_caller(manager: McpManager, server: str, tool: str):
    def _call(**kwargs: Any) -> str:
        return manager.call(server, tool, kwargs)

    return _call
