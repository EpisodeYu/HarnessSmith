# Environment variables for {{ project_slug }}.
# Copy this file to .env (which is gitignored) and fill in real values.
# Config and harness.spec.yaml only ever store these NAMES, never the values.
{% for name in env_names %}{{ name }}=
{% endfor %}